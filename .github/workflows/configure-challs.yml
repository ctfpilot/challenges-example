name: Configure challs

on:
  push:
    paths:
      - "challenges/**"
  workflow_dispatch:

jobs:
  configure_challenges:
    name: Configure challenges
    uses: ctfpilot/challenge-ci/.github/workflows/configure-challs.yml@eb1cb68bd7d88a0306481c924aa1018838e2bb4d
    permissions:
      contents: write
      packages: write
      id-token: write
    with:
      runs-on: "ubuntu-latest"
